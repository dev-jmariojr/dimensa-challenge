server:
  port: 8989
app:
  name: Challenge Dimensa Contacts API
spring:
  datasource:
    url: ${JDBC_DATASOURCE_URL}
    username: postgres
    password: postgres
  jpa:
#    show-sql: true
    hibernate:
      ddl-auto: 'create-drop'
